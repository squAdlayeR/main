language: swift

branches:
 only:
 - master

xcode_workspace: lAyeR.xcworkspace
xcode_scheme: lAyeRTests
osx_image: xcode6.4
xcode_sdk: iphonesimulator

script:
- xctool clean build test -workspace /lAyeR.xcworkspace -scheme lAyeRTests
