language: swift

branches:
 only:
 - master

xcode_workspace: lAyeR.xcworkspace
xcode_scheme: lAyeRTests
osx_image: beta-xcode6.3
xcode_sdk: iphonesimulator8.3

script:
- xcodebuild clean build test -project lAyeR.xcworkspace -scheme lAyeRTests
