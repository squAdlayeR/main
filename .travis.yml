language: swift

# branches:
#  only:
#  - master

xcode_workspace: lAyeR.xcworkspace
xcode_scheme: lAyeRTests
osx_image: xcode8.1
xcode_sdk: iphonesimulator

script:
- xcodebuild clean build test -workspace lAyeR/lAyeR.xcworkspace -scheme lAyeRTests
